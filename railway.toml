[build]
builder = "NIXPACKS"
buildCommand = "cd python && pip install -e ."

[deploy]
startCommand = "cd python && uvicorn entertainment_graph.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
